# Load Packages -----

library(magrittr)
library(ragg)

# Set Options -----

options(scipen = 999)
options(vsc.use_httpgd = TRUE)
options(langserver_quiet = TRUE)
options(repos=c(CRAN="https://vps.fmvz.usp.br/CRAN/"))
options(interrupt = function() return(invisible()))

# Positron Fix -----

Sys.setenv(R_HOME="/usr/lib64/R")
Sys.setenv(R_SHARE_DIR="/usr/share/R/")
Sys.setenv(R_DOC_DIR="/usr/share/doc/R/")
Sys.setenv(R_INCLUDE_DIR="/usr/include/R/")
